class Package {
    name: String
    version: String = "1.0.0"
    deprecated: Boolean = false

    fixed artifactName = "package_\(name.replaceAll("/", "_"))"
    fixed nameLastPart = name.split("/").last
}

package = new Listing<Package> {
    new Package {
        name = "actions/checkout@v4"
    }
}