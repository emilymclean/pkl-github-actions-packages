actions/checkout@v4:
  inputs:
    repository: String?
    ref: String?
    token: String?
    ssh-strict: Boolean?
    ssh-user: String?
    persist-credentials: Boolean?
    path: String?
    clean: Boolean?
    sparse-checkout-cone-mode: Boolean?
    fetch-depth: Int?
    fetch-tags: Boolean?
    show-progress: Boolean?
    lfs: Boolean?
    submodules: Boolean?
    set-safe-directory: Boolean?
    github-server-url: Boolean?